export interface iFriend {
  id: number;
  name: string;
  email: string;
  phone: number;
  balance: number;

  isActive: boolean;

  // createdAt: number; createdBy: string;
  // updatedAt: number; updatedBy: string;
}